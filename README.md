# Velocity Analytics

Spring Boot service for uploading Jira or Bitbucket delivery reports and returning dashboard analytics.

Run:

```bash
./gradlew bootRun
```

Endpoint:

`POST /api/jira/upload`

Form field:

`file`
